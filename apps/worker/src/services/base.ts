/**
 * Placeholder directories kept as explicit modules so the intended
 * structure is visible at foundation stage:
 *   services/ — job orchestration helpers (future)
 *   lib/      — shared worker utilities
 *   utils/    — pure helpers
 */
export {};
