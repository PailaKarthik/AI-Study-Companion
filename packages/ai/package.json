{
  "name": "@ai-study-companion/ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ai-study-companion/shared": "workspace:*"
  },
  "devDependencies": {
    "@ai-study-companion/db": "workspace:*",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
